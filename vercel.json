{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "buildCommand": "yarn build",
    "installCommand": "corepack prepare yarn@4.6.0 --activate && yarn install"
}